<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:VrChatCompanion.ViewModels;assembly=VrChatCompanion"
             x:Class="VrChatCompanion.Views.HomePage"
             Shell.NavBarIsVisible="True"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <viewModels:HomeViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout Orientation="Horizontal">
                <Frame CornerRadius="500" Margin="10" Padding="-10" WidthRequest="150">
                    <Image Source="{Binding UserIcon, Mode=TwoWay}" />
                </Frame>
                <Label Text="{Binding Text}"
                       TextColor="{StaticResource Primary}"
                       FontSize="18"
                       VerticalOptions="CenterAndExpand" 
                       HorizontalOptions="StartAndExpand" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>